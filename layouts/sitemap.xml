{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
  {{- range .Pages }}
  {{- $p := . }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
    {{- end }}
    {{- /* Das Cover wandert als Bild-Eintrag mit, in der Größe, die auch
         die Seite ausliefert; so entsteht keine zweite Bildvariante. */}}
    {{- with .Params.cover }}{{ with $p.Resources.GetMatch . }}
    <image:image>
      <image:loc>{{ (.Resize "1600x webp q80").Permalink }}</image:loc>
    </image:image>
    {{- end }}{{ end }}
  </url>
  {{- end }}
</urlset>
