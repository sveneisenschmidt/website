{{- $pages := sort (where .Site.RegularPages "Section" "in" (slice "photos" "trips" "notes" "essays")) ".Date" "desc" -}}

{{- $frozen := dict -}}
{{- range hugo.Data.redirects -}}
  {{- if eq .rss "keep" -}}
    {{- $frozen = merge $frozen (dict .to (printf "%s%s" (strings.TrimSuffix "/" site.BaseURL) .from)) -}}
  {{- end -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
  {{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ .Site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ .Site.Params.description | default .Site.Title }}</description>
    <generator>Hugo</generator>
    <language>{{ .Site.Language.Locale }}</language>
    <lastBuildDate>{{ (index $pages 0).Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" }}</lastBuildDate>
    <atom:link href="{{ .Permalink }}index.xml" rel="self" type="application/rss+xml" />
    {{- range $pages }}
    {{- $link := printf "%s?source=rss" .Permalink }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ $link }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" }}</pubDate>
      <guid isPermaLink="true">{{ with index $frozen .RelPermalink }}{{ printf "%s?source=rss" . }}{{ else }}{{ $link }}{{ end }}</guid>
      <description>{{ printf "<![CDATA[" | safeHTML }}{{- if .Params.cover -}}
          {{- $img := .Resources.GetMatch .Params.cover -}}
          {{- if $img -}}
            {{- $resized := $img.Fit "768x768 webp q80" -}}
            <img src="{{ $resized.Permalink }}" width="{{ $resized.Width }}" height="{{ $resized.Height }}" alt="{{ .Title }} cover image" />
          {{- end -}}
        {{- end -}}
        <p>{{ .Summary | plainify | htmlUnescape }}{{- $contentWords := countwords .Content -}}
        {{- $summaryWords := countwords .Summary -}}
        {{- $remainingWords := sub $contentWords $summaryWords -}}
        {{- if gt $remainingWords 0 }} (<a href="{{ $link }}" title="Continue reading {{ .Title }}">{{ $remainingWords }} more words</a>){{- end -}}</p>{{ printf "]]>" | safeHTML }}</description>
    </item>
    {{- end }}
  </channel>
</rss>
